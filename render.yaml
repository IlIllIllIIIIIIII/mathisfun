services:
  - type: web
    name: mathisfun
    runtime: static
    buildCommand: npm ci --include=dev && npm run build:render
    staticPublishPath: ./dist-render
    envVars:
      - key: NODE_VERSION
        value: "22"
